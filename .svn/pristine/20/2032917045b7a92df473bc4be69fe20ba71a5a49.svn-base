import userList from 'static/json/lineTime';

export default {
    lineTimer: config => {
        // console.log(userList);
        let timeData = {
            lineTime:userList
        }
        return timeData
    }
}